name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      version:
        description: "Version to build, without the leading v"
        required: true

permissions:
  contents: write

jobs:
  package:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux
            os: ubuntu-22.04   # oldest supported glibc, so the tarball runs widely
          - name: Windows
            os: windows-2022

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - run: python -m pip install --upgrade pip pybind11 numpy

      - name: Linux system dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential cmake ninja-build file \
            qt6-base-dev qt6-charts-dev qt6-svg-dev \
            libeigen3-dev nlohmann-json3-dev python3-dev

      - name: Qt
        if: runner.os == 'Windows'
        uses: jurplel/install-qt-action@v4
        with:
          version: "6.6.3"
          modules: qtcharts
          cache: true

      - name: Windows header-only dependencies
        if: runner.os == 'Windows'
        shell: bash
        run: |
          # Both are headers, but Eigen ships no usable Eigen3Config.cmake in
          # its source tarball, so it is installed rather than pointed at.
          # nlohmann is found by the header search that CMakeLists falls back
          # to, which looks under <prefix>/include.
          mkdir -p "$RUNNER_TEMP/src"
          curl -sL https://gitlab.com/libeigen/eigen/-/archive/3.4.0/eigen-3.4.0.tar.gz \
            | tar xz -C "$RUNNER_TEMP/src"
          cmake -S "$RUNNER_TEMP/src/eigen-3.4.0" -B "$RUNNER_TEMP/eigen-build" \
            -DCMAKE_INSTALL_PREFIX="$RUNNER_TEMP/prefix" \
            -DBUILD_TESTING=OFF -DEIGEN_BUILD_DOC=OFF
          cmake --install "$RUNNER_TEMP/eigen-build"

          curl -sL -o "$RUNNER_TEMP/src/json.tar.xz" \
            https://github.com/nlohmann/json/releases/download/v3.11.3/json.tar.xz
          tar xf "$RUNNER_TEMP/src/json.tar.xz" -C "$RUNNER_TEMP/src"
          cp -r "$RUNNER_TEMP/src/json/include/nlohmann" "$RUNNER_TEMP/prefix/include/"

      - uses: ilammy/msvc-dev-cmd@v1
        if: runner.os == 'Windows'

      - name: Configure and build
        shell: bash
        run: |
          EXTRA=""
          if [ "$RUNNER_OS" = "Windows" ]; then
            EXTRA="-DCMAKE_PREFIX_PATH=$RUNNER_TEMP/prefix"
          fi
          cmake -S . -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DPython3_EXECUTABLE="$(python -c 'import sys; print(sys.executable)')" \
            $EXTRA
          cmake --build build --parallel

      # Shipping something that was never run is how a broken release happens.
      - name: Test
        working-directory: build
        run: ctest --output-on-failure --timeout 180

      - name: Package (Linux)
        if: runner.os == 'Linux'
        working-directory: build
        run: cpack -G "TGZ;DEB"

      # windeployqt copies the Qt DLLs and plugins next to the executable, and
      # the Python DLL goes with them: the application embeds the interpreter,
      # so without it the package will not start at all.
      - name: Stage Qt and Python runtime (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          windeployqt --release --no-translations --no-system-d3d-compiler \
            build/bin/simupy.exe
          PYDLL=$(python -c "import sys,os;print(os.path.join(sys.base_prefix,f'python{sys.version_info.major}{sys.version_info.minor}.dll'))")
          cp "$PYDLL" build/bin/

      - name: Package (Windows)
        if: runner.os == 'Windows'
        working-directory: build
        run: cpack -G "ZIP;NSIS"

      - name: Collect assets
        shell: bash
        run: |
          mkdir -p assets
          find build -maxdepth 1 \
            \( -name "simupy*.tar.gz" -o -name "simupy*.deb" \
               -o -name "simupy*.zip" -o -name "simupy*.exe" \) \
            -exec cp {} assets/ \;
          ls -lh assets

      - uses: actions/upload-artifact@v4
        with:
          name: simupy-${{ matrix.name }}
          path: assets/*
          if-no-files-found: error

  release:
    name: Publish
    needs: package
    runs-on: ubuntu-24.04
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          path: assets
          merge-multiple: true

      - name: Checksums
        run: |
          cd assets
          sha256sum * > SHA256SUMS
          cat SHA256SUMS

      - uses: softprops/action-gh-release@v2
        with:
          files: assets/*
          generate_release_notes: true
          body: |
            ## Installing

            **Linux (Debian, Ubuntu)** — `sudo apt install ./simupy_*_amd64.deb`.
            Qt, Python and NumPy come in as dependencies.

            **Linux (any distribution)** — unpack the `.tar.gz`. It needs Qt 6.2+,
            Python 3.8+ and NumPy already present.

            **Windows** — run the `.exe` installer, or unpack the `.zip` to run it
            in place. Qt and the Python runtime are bundled; install NumPy with
            `pip install numpy` for Python blocks to work.

            `pyserial` is only needed for the Arduino and serial blocks.
